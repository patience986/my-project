doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Receipts Overview - KGL
  style.
    body { font-family: Arial, sans-serif;  background-image: url("images/p13.jpeg"); }
    header { background-color:  lightsalmon; color: white; padding: 10px 0; text-align: center; }
    header nav a { color: white; margin: 0 15px; text-decoration: none; }
    .container { margin: 20px; }
    table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
    table th, table td { border: 1px solid #ddd; padding: 10px; text-align: left; }
    table th { background-color:  lightsalmon; color: white; cursor: pointer; }
    table tr:nth-child(even) { background-color: #f2f2f2; }
    table tr:hover { background-color: #ddd; }
    input[type="text"] { padding: 10px; width: 100%; margin-bottom: 20px; box-sizing: border-box; }
header
  h1 Receipts Overview - KGL
  nav
    a(href='dashboard.html') Dashboard
    a(href='stockmanagement.html') Stock Management
    a(href='') Sales Records
    a(href='creditsales.html') Credit Sales
    a(href='logout.html') Logout
.container
  h2 Receipts Overview
  // Receipt Overview Table
  table#receiptsTable(action="/receiptoverview" method="post")
    thead
      tr
        th(onclick='sortTable(0)') Receipt Number
        th(onclick='sortTable(1)') Date
        th(onclick='sortTable(2)') Product Name
        th(onclick='sortTable(3)') Tonnage (kg)
        th(onclick='sortTable(4)') Amount Paid (UGX)
        th(onclick='sortTable(5)') Buyer&rsquo;s Name
        th(onclick='sortTable(6)') Sales Agent
        th(onclick='sortTable(7)') Branch
        th Actions
    tbody
      tr
        td 1
        td 2024-08-24
        td Rice
        td 500
        td 3000000
        td Jane friend
        td Immaculate
        td Maganjo
        td
          a(href='receipt-details.html') View
      tr
        td 2
        td 2024-08-23
        td Beans
        td 300
        td 1800000
        td Prossy
        td Desire
        td Wakiso
        td
          a(href='receipt-details.html') View
      // Additional receipt rows can be added here
script.
  // Search Function
  function searchReceipts() {
  const input = document.getElementById("searchInput").value.toUpperCase();
  const table = document.getElementById("receiptsTable");
  const rows = table.getElementsByTagName("tr");
  for (let i = 1; i < rows.length; i++) {
  let isVisible = false;
  const cells = rows[i].getElementsByTagName("td");
  for (let j = 0; j < cells.length; j++) {
  if (cells[j].textContent.toUpperCase().includes(input)) {
  isVisible = true;
  break;
  }
  }
  rows[i].style.display = isVisible ? "" : "none";
  }
  }
  // Sort Function
  function sortTable(n) {
  const table = document.getElementById("receiptsTable");
  const rows = Array.from(table.getElementsByTagName("tr"));
  const headerRow = rows.shift(); // Remove the header row from sorting
  let sortedRows = rows.sort((rowA, rowB) => {
  const cellA = rowA.getElementsByTagName("td")[n].textContent.toLowerCase();
  const cellB = rowB.getElementsByTagName("td")[n].textContent.toLowerCase();
  return cellA.localeCompare(cellB);
  });
  const tableBody = table.getElementsByTagName("tbody")[0];
  tableBody.innerHTML = ""; // Clear current table content
  tableBody.appendChild(headerRow); // Reinsert the header row
  sortedRows.forEach(row => tableBody.appendChild(row)); // Reinsert sorted rows
  }
